# lol-challenges-exporter

The repo contains a Node.js script that connects to the running League client (LCU API) and exports which champions you've completed per challenge.

```bash
cd tools
npm install
node export.mjs
```

This generates a JSON file on your Desktop that you can upload via the **Import Champion Data** option in Settings.
